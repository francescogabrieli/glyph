# Backend Agent Instructions

Read the relevant service and test files before editing.
Keep changes small and follow the existing FastAPI style.
Run `pytest` before saying the task is complete.
Report what changed and what was verified.

Never commit secrets, credentials, tokens, or API keys.
Ask for confirmation before destructive operations or production configuration changes.
